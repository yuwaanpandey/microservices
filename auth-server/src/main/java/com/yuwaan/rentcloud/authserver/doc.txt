spring:
  security:
    user:
      name: rahul
      password: rahul
      roles: ADMIN, USER
security:
  oauth2:
    client:
      client-id: mobile
      client-secret: pin
      access-token-validity-seconds: 3600
      authorized-grant-types: refresh_token,authorization_code,password,client_credentials
      scope: READ,WRITE
    authorization:
      check-token-access: permitAll


The above is a configuration which was in yml file, now we will get this in code.